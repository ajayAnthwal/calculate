import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata = {
  title: "Publish Your Book With - Best Book Publishers In Maharashtra",
  description:
    "Get your book published with the most trusted Book Publishers In Maharashtra. Manuscript to Bookshelf in 4 weeks. keep 100% Profit. keep 100% Profit. Automated Sales Report. Book Publishing.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
